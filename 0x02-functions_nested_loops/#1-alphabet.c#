#include "main.h"

/**
*main - Entry point
*
*description - to print an alphabet in lower case
*
*return: Always return 0 - (success)
*/
int main(void)
{
int x = 'a'
while(x <= 'z')
_putchar(x);
x++;
return(0);
}